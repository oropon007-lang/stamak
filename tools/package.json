{
  "name": "stamak-tools",
  "version": "1.0.0",
  "description": "LINE sticker maker pipeline (crop -> AI bg removal -> finalize)",
  "type": "module",
  "private": true,
  "scripts": {
    "crop": "node --env-file-if-exists=../.env crop.mjs",
    "process": "node postprocess.mjs",
    "apply-rename": "node apply-rename.mjs",
    "build": "npm run crop && npm run process && npm run apply-rename",
    "clear": "node clear.mjs",
    "clear:final": "node clear.mjs --stickers",
    "clear:cache": "node clear.mjs --cache",
    "retry": "npm run clear && npm run build",
    "rebuild:final": "npm run clear:final && npm run process"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
