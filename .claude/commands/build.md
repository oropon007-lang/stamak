---
description: Run the full sticker pipeline (crop + transparency processing)
allowed-tools: Bash
---

Run the LINE sticker build pipeline:

```
cd ~/projects/stamak/tools && npm run build
```

After it finishes, briefly summarize what was produced (which sheets, total sticker count, where output went). Do not visually inspect outputs unless asked.
